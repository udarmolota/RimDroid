<?xml version="1.0" encoding="utf-8"?>
<!-- Wrap the logo PNG with inset so the full image fits inside the adaptive
     icon safe zone (center 72dp of the 108dp canvas, i.e. ~33% padding).
     Adjust android:inset to taste: bigger = smaller logo, more padding. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/rimdroid_logo"
    android:inset="12%"/>
